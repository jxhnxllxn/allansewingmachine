import React from 'react'
import { Route, Switch } from 'react-router-dom'
import './dashboard.scss'

const UserDashboard = () => {
    return (
        <div>
            {/* <Switch>
                <Route path={`${match.path}`} component={account} exact />
                <Route path={`${match.path}/checkot`} component={} exact/>
            </Switch> */}
        </div>
    )
}

export default UserDashboard
