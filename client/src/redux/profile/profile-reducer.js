const inititalState = {
    profile:null,
    order:[],
    loading:true,
    error:{}
}

export default function(state = inititalState, action){
    // const {type,payload} = action;

    // switch (key) {
    //     case value:
            
    //         break;
    
    //     default:
    //         break;
    // }
}